{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npm run prisma:generate && npm run build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.js"
    }
  ]
}
